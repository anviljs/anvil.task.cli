## Anvil Task Command Line Interface

This extension is a core component of anvil and is required in order to interact with tasks from the command line.

## Installation

anvil will install this plugin during post-install.